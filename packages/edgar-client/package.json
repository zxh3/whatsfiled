{
  "name": "@whatsfiled/edgar-client",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest run --coverage",
    "fixtures:generate": "tsx scripts/generate-fixtures.ts"
  },
  "dependencies": {
    "fast-xml-parser": "^5.3.3"
  },
  "devDependencies": {
    "@whatsfiled/typescript-config": "workspace:*",
    "@types/node": "^20",
    "@vitest/coverage-v8": "^3.0.0",
    "@vitest/ui": "^3.0.0",
    "typescript": "^5",
    "vitest": "^3.0.0"
  }
}
