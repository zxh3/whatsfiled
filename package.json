{
  "name": "whatsfiled",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "biome check .",
    "typecheck": "turbo typecheck",
    "test": "turbo test",
    "format": "biome format --write",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "docker:reset": "docker compose down -v && docker compose up -d",
    "db:push": "pnpm --filter @whatsfiled/db db:push",
    "db:studio": "pnpm --filter @whatsfiled/db db:studio",
    "db:sync": "./scripts/db-sync.sh"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.13",
    "turbo": "^2.7.6",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
